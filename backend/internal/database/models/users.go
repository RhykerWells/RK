// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// User is an object representing the database table.
type User struct {
	ID           int64       `boil:"id" json:"id" toml:"id" yaml:"id"`
	GlobalRoleID int64       `boil:"global_role_id" json:"global_role_id" toml:"global_role_id" yaml:"global_role_id"`
	AuthType     string      `boil:"auth_type" json:"auth_type" toml:"auth_type" yaml:"auth_type"`
	Username     null.String `boil:"username" json:"username,omitempty" toml:"username" yaml:"username,omitempty"`
	PasswordHash null.String `boil:"password_hash" json:"password_hash,omitempty" toml:"password_hash" yaml:"password_hash,omitempty"`
	DiscordID    null.String `boil:"discord_id" json:"discord_id,omitempty" toml:"discord_id" yaml:"discord_id,omitempty"`
	DisplayName  string      `boil:"display_name" json:"display_name" toml:"display_name" yaml:"display_name"`
	Email        null.String `boil:"email" json:"email,omitempty" toml:"email" yaml:"email,omitempty"`
	AvatarURL    null.String `boil:"avatar_url" json:"avatar_url,omitempty" toml:"avatar_url" yaml:"avatar_url,omitempty"`
	IsActive     bool        `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	LastLoginAt  null.Time   `boil:"last_login_at" json:"last_login_at,omitempty" toml:"last_login_at" yaml:"last_login_at,omitempty"`
	CreatedAt    time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *userR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L userL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var UserColumns = struct {
	ID           string
	GlobalRoleID string
	AuthType     string
	Username     string
	PasswordHash string
	DiscordID    string
	DisplayName  string
	Email        string
	AvatarURL    string
	IsActive     string
	LastLoginAt  string
	CreatedAt    string
	UpdatedAt    string
}{
	ID:           "id",
	GlobalRoleID: "global_role_id",
	AuthType:     "auth_type",
	Username:     "username",
	PasswordHash: "password_hash",
	DiscordID:    "discord_id",
	DisplayName:  "display_name",
	Email:        "email",
	AvatarURL:    "avatar_url",
	IsActive:     "is_active",
	LastLoginAt:  "last_login_at",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
}

var UserTableColumns = struct {
	ID           string
	GlobalRoleID string
	AuthType     string
	Username     string
	PasswordHash string
	DiscordID    string
	DisplayName  string
	Email        string
	AvatarURL    string
	IsActive     string
	LastLoginAt  string
	CreatedAt    string
	UpdatedAt    string
}{
	ID:           "users.id",
	GlobalRoleID: "users.global_role_id",
	AuthType:     "users.auth_type",
	Username:     "users.username",
	PasswordHash: "users.password_hash",
	DiscordID:    "users.discord_id",
	DisplayName:  "users.display_name",
	Email:        "users.email",
	AvatarURL:    "users.avatar_url",
	IsActive:     "users.is_active",
	LastLoginAt:  "users.last_login_at",
	CreatedAt:    "users.created_at",
	UpdatedAt:    "users.updated_at",
}

// Generated where

type whereHelperbool struct{ field string }

func (w whereHelperbool) EQ(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperbool) NEQ(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperbool) LT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperbool) LTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperbool) GT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperbool) GTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

type whereHelpernull_Time struct{ field string }

func (w whereHelpernull_Time) EQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Time) NEQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Time) LT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Time) LTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Time) GT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Time) GTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpernull_Time) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Time) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var UserWhere = struct {
	ID           whereHelperint64
	GlobalRoleID whereHelperint64
	AuthType     whereHelperstring
	Username     whereHelpernull_String
	PasswordHash whereHelpernull_String
	DiscordID    whereHelpernull_String
	DisplayName  whereHelperstring
	Email        whereHelpernull_String
	AvatarURL    whereHelpernull_String
	IsActive     whereHelperbool
	LastLoginAt  whereHelpernull_Time
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
}{
	ID:           whereHelperint64{field: "\"users\".\"id\""},
	GlobalRoleID: whereHelperint64{field: "\"users\".\"global_role_id\""},
	AuthType:     whereHelperstring{field: "\"users\".\"auth_type\""},
	Username:     whereHelpernull_String{field: "\"users\".\"username\""},
	PasswordHash: whereHelpernull_String{field: "\"users\".\"password_hash\""},
	DiscordID:    whereHelpernull_String{field: "\"users\".\"discord_id\""},
	DisplayName:  whereHelperstring{field: "\"users\".\"display_name\""},
	Email:        whereHelpernull_String{field: "\"users\".\"email\""},
	AvatarURL:    whereHelpernull_String{field: "\"users\".\"avatar_url\""},
	IsActive:     whereHelperbool{field: "\"users\".\"is_active\""},
	LastLoginAt:  whereHelpernull_Time{field: "\"users\".\"last_login_at\""},
	CreatedAt:    whereHelpertime_Time{field: "\"users\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"users\".\"updated_at\""},
}

// UserRels is where relationship names are stored.
var UserRels = struct {
	GlobalRole                 string
	CreatedByDocumentFiles     string
	CreatedByDocumentLinks     string
	CreatedByDocumentVersions  string
	CreatedByDocuments         string
	OwnerDocuments             string
	UpdatedByDocuments         string
	UploadedByFiles            string
	CreatedByFolderPermissions string
	FolderPermissions          string
	CreatedByFolders           string
	UpdatedByFolders           string
	PortalMemberships          string
	CreatedByPortalRoles       string
	UpdatedByPortalRoles       string
	CreatedByPortals           string
	UpdatedByPortals           string
}{
	GlobalRole:                 "GlobalRole",
	CreatedByDocumentFiles:     "CreatedByDocumentFiles",
	CreatedByDocumentLinks:     "CreatedByDocumentLinks",
	CreatedByDocumentVersions:  "CreatedByDocumentVersions",
	CreatedByDocuments:         "CreatedByDocuments",
	OwnerDocuments:             "OwnerDocuments",
	UpdatedByDocuments:         "UpdatedByDocuments",
	UploadedByFiles:            "UploadedByFiles",
	CreatedByFolderPermissions: "CreatedByFolderPermissions",
	FolderPermissions:          "FolderPermissions",
	CreatedByFolders:           "CreatedByFolders",
	UpdatedByFolders:           "UpdatedByFolders",
	PortalMemberships:          "PortalMemberships",
	CreatedByPortalRoles:       "CreatedByPortalRoles",
	UpdatedByPortalRoles:       "UpdatedByPortalRoles",
	CreatedByPortals:           "CreatedByPortals",
	UpdatedByPortals:           "UpdatedByPortals",
}

// userR is where relationships are stored.
type userR struct {
	GlobalRole                 *GlobalRole           `boil:"GlobalRole" json:"GlobalRole" toml:"GlobalRole" yaml:"GlobalRole"`
	CreatedByDocumentFiles     DocumentFileSlice     `boil:"CreatedByDocumentFiles" json:"CreatedByDocumentFiles" toml:"CreatedByDocumentFiles" yaml:"CreatedByDocumentFiles"`
	CreatedByDocumentLinks     DocumentLinkSlice     `boil:"CreatedByDocumentLinks" json:"CreatedByDocumentLinks" toml:"CreatedByDocumentLinks" yaml:"CreatedByDocumentLinks"`
	CreatedByDocumentVersions  DocumentVersionSlice  `boil:"CreatedByDocumentVersions" json:"CreatedByDocumentVersions" toml:"CreatedByDocumentVersions" yaml:"CreatedByDocumentVersions"`
	CreatedByDocuments         DocumentSlice         `boil:"CreatedByDocuments" json:"CreatedByDocuments" toml:"CreatedByDocuments" yaml:"CreatedByDocuments"`
	OwnerDocuments             DocumentSlice         `boil:"OwnerDocuments" json:"OwnerDocuments" toml:"OwnerDocuments" yaml:"OwnerDocuments"`
	UpdatedByDocuments         DocumentSlice         `boil:"UpdatedByDocuments" json:"UpdatedByDocuments" toml:"UpdatedByDocuments" yaml:"UpdatedByDocuments"`
	UploadedByFiles            FileSlice             `boil:"UploadedByFiles" json:"UploadedByFiles" toml:"UploadedByFiles" yaml:"UploadedByFiles"`
	CreatedByFolderPermissions FolderPermissionSlice `boil:"CreatedByFolderPermissions" json:"CreatedByFolderPermissions" toml:"CreatedByFolderPermissions" yaml:"CreatedByFolderPermissions"`
	FolderPermissions          FolderPermissionSlice `boil:"FolderPermissions" json:"FolderPermissions" toml:"FolderPermissions" yaml:"FolderPermissions"`
	CreatedByFolders           FolderSlice           `boil:"CreatedByFolders" json:"CreatedByFolders" toml:"CreatedByFolders" yaml:"CreatedByFolders"`
	UpdatedByFolders           FolderSlice           `boil:"UpdatedByFolders" json:"UpdatedByFolders" toml:"UpdatedByFolders" yaml:"UpdatedByFolders"`
	PortalMemberships          PortalMembershipSlice `boil:"PortalMemberships" json:"PortalMemberships" toml:"PortalMemberships" yaml:"PortalMemberships"`
	CreatedByPortalRoles       PortalRoleSlice       `boil:"CreatedByPortalRoles" json:"CreatedByPortalRoles" toml:"CreatedByPortalRoles" yaml:"CreatedByPortalRoles"`
	UpdatedByPortalRoles       PortalRoleSlice       `boil:"UpdatedByPortalRoles" json:"UpdatedByPortalRoles" toml:"UpdatedByPortalRoles" yaml:"UpdatedByPortalRoles"`
	CreatedByPortals           PortalSlice           `boil:"CreatedByPortals" json:"CreatedByPortals" toml:"CreatedByPortals" yaml:"CreatedByPortals"`
	UpdatedByPortals           PortalSlice           `boil:"UpdatedByPortals" json:"UpdatedByPortals" toml:"UpdatedByPortals" yaml:"UpdatedByPortals"`
}

// NewStruct creates a new relationship struct
func (*userR) NewStruct() *userR {
	return &userR{}
}

func (o *User) GetGlobalRole() *GlobalRole {
	if o == nil {
		return nil
	}

	return o.R.GetGlobalRole()
}

func (r *userR) GetGlobalRole() *GlobalRole {
	if r == nil {
		return nil
	}

	return r.GlobalRole
}

func (o *User) GetCreatedByDocumentFiles() DocumentFileSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByDocumentFiles()
}

func (r *userR) GetCreatedByDocumentFiles() DocumentFileSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByDocumentFiles
}

func (o *User) GetCreatedByDocumentLinks() DocumentLinkSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByDocumentLinks()
}

func (r *userR) GetCreatedByDocumentLinks() DocumentLinkSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByDocumentLinks
}

func (o *User) GetCreatedByDocumentVersions() DocumentVersionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByDocumentVersions()
}

func (r *userR) GetCreatedByDocumentVersions() DocumentVersionSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByDocumentVersions
}

func (o *User) GetCreatedByDocuments() DocumentSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByDocuments()
}

func (r *userR) GetCreatedByDocuments() DocumentSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByDocuments
}

func (o *User) GetOwnerDocuments() DocumentSlice {
	if o == nil {
		return nil
	}

	return o.R.GetOwnerDocuments()
}

func (r *userR) GetOwnerDocuments() DocumentSlice {
	if r == nil {
		return nil
	}

	return r.OwnerDocuments
}

func (o *User) GetUpdatedByDocuments() DocumentSlice {
	if o == nil {
		return nil
	}

	return o.R.GetUpdatedByDocuments()
}

func (r *userR) GetUpdatedByDocuments() DocumentSlice {
	if r == nil {
		return nil
	}

	return r.UpdatedByDocuments
}

func (o *User) GetUploadedByFiles() FileSlice {
	if o == nil {
		return nil
	}

	return o.R.GetUploadedByFiles()
}

func (r *userR) GetUploadedByFiles() FileSlice {
	if r == nil {
		return nil
	}

	return r.UploadedByFiles
}

func (o *User) GetCreatedByFolderPermissions() FolderPermissionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByFolderPermissions()
}

func (r *userR) GetCreatedByFolderPermissions() FolderPermissionSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByFolderPermissions
}

func (o *User) GetFolderPermissions() FolderPermissionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetFolderPermissions()
}

func (r *userR) GetFolderPermissions() FolderPermissionSlice {
	if r == nil {
		return nil
	}

	return r.FolderPermissions
}

func (o *User) GetCreatedByFolders() FolderSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByFolders()
}

func (r *userR) GetCreatedByFolders() FolderSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByFolders
}

func (o *User) GetUpdatedByFolders() FolderSlice {
	if o == nil {
		return nil
	}

	return o.R.GetUpdatedByFolders()
}

func (r *userR) GetUpdatedByFolders() FolderSlice {
	if r == nil {
		return nil
	}

	return r.UpdatedByFolders
}

func (o *User) GetPortalMemberships() PortalMembershipSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPortalMemberships()
}

func (r *userR) GetPortalMemberships() PortalMembershipSlice {
	if r == nil {
		return nil
	}

	return r.PortalMemberships
}

func (o *User) GetCreatedByPortalRoles() PortalRoleSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByPortalRoles()
}

func (r *userR) GetCreatedByPortalRoles() PortalRoleSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByPortalRoles
}

func (o *User) GetUpdatedByPortalRoles() PortalRoleSlice {
	if o == nil {
		return nil
	}

	return o.R.GetUpdatedByPortalRoles()
}

func (r *userR) GetUpdatedByPortalRoles() PortalRoleSlice {
	if r == nil {
		return nil
	}

	return r.UpdatedByPortalRoles
}

func (o *User) GetCreatedByPortals() PortalSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByPortals()
}

func (r *userR) GetCreatedByPortals() PortalSlice {
	if r == nil {
		return nil
	}

	return r.CreatedByPortals
}

func (o *User) GetUpdatedByPortals() PortalSlice {
	if o == nil {
		return nil
	}

	return o.R.GetUpdatedByPortals()
}

func (r *userR) GetUpdatedByPortals() PortalSlice {
	if r == nil {
		return nil
	}

	return r.UpdatedByPortals
}

// userL is where Load methods for each relationship are stored.
type userL struct{}

var (
	userAllColumns            = []string{"id", "global_role_id", "auth_type", "username", "password_hash", "discord_id", "display_name", "email", "avatar_url", "is_active", "last_login_at", "created_at", "updated_at"}
	userColumnsWithoutDefault = []string{"global_role_id", "auth_type", "display_name"}
	userColumnsWithDefault    = []string{"id", "username", "password_hash", "discord_id", "email", "avatar_url", "is_active", "last_login_at", "created_at", "updated_at"}
	userPrimaryKeyColumns     = []string{"id"}
	userGeneratedColumns      = []string{"id"}
)

type (
	// UserSlice is an alias for a slice of pointers to User.
	// This should almost always be used instead of []User.
	UserSlice []*User

	userQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	userType                 = reflect.TypeOf(&User{})
	userMapping              = queries.MakeStructMapping(userType)
	userPrimaryKeyMapping, _ = queries.BindMapping(userType, userMapping, userPrimaryKeyColumns)
	userInsertCacheMut       sync.RWMutex
	userInsertCache          = make(map[string]insertCache)
	userUpdateCacheMut       sync.RWMutex
	userUpdateCache          = make(map[string]updateCache)
	userUpsertCacheMut       sync.RWMutex
	userUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single user record from the query.
func (q userQuery) One(ctx context.Context, exec boil.ContextExecutor) (*User, error) {
	o := &User{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for users")
	}

	return o, nil
}

// All returns all User records from the query.
func (q userQuery) All(ctx context.Context, exec boil.ContextExecutor) (UserSlice, error) {
	var o []*User

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to User slice")
	}

	return o, nil
}

// Count returns the count of all User records in the query.
func (q userQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count users rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q userQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if users exists")
	}

	return count > 0, nil
}

// GlobalRole pointed to by the foreign key.
func (o *User) GlobalRole(mods ...qm.QueryMod) globalRoleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.GlobalRoleID),
	}

	queryMods = append(queryMods, mods...)

	return GlobalRoles(queryMods...)
}

// CreatedByDocumentFiles retrieves all the document_file's DocumentFiles with an executor via created_by column.
func (o *User) CreatedByDocumentFiles(mods ...qm.QueryMod) documentFileQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"document_files\".\"created_by\"=?", o.ID),
	)

	return DocumentFiles(queryMods...)
}

// CreatedByDocumentLinks retrieves all the document_link's DocumentLinks with an executor via created_by column.
func (o *User) CreatedByDocumentLinks(mods ...qm.QueryMod) documentLinkQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"document_links\".\"created_by\"=?", o.ID),
	)

	return DocumentLinks(queryMods...)
}

// CreatedByDocumentVersions retrieves all the document_version's DocumentVersions with an executor via created_by column.
func (o *User) CreatedByDocumentVersions(mods ...qm.QueryMod) documentVersionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"document_versions\".\"created_by\"=?", o.ID),
	)

	return DocumentVersions(queryMods...)
}

// CreatedByDocuments retrieves all the document's Documents with an executor via created_by column.
func (o *User) CreatedByDocuments(mods ...qm.QueryMod) documentQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"documents\".\"created_by\"=?", o.ID),
	)

	return Documents(queryMods...)
}

// OwnerDocuments retrieves all the document's Documents with an executor via owner_id column.
func (o *User) OwnerDocuments(mods ...qm.QueryMod) documentQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"documents\".\"owner_id\"=?", o.ID),
	)

	return Documents(queryMods...)
}

// UpdatedByDocuments retrieves all the document's Documents with an executor via updated_by column.
func (o *User) UpdatedByDocuments(mods ...qm.QueryMod) documentQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"documents\".\"updated_by\"=?", o.ID),
	)

	return Documents(queryMods...)
}

// UploadedByFiles retrieves all the file's Files with an executor via uploaded_by column.
func (o *User) UploadedByFiles(mods ...qm.QueryMod) fileQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"files\".\"uploaded_by\"=?", o.ID),
	)

	return Files(queryMods...)
}

// CreatedByFolderPermissions retrieves all the folder_permission's FolderPermissions with an executor via created_by column.
func (o *User) CreatedByFolderPermissions(mods ...qm.QueryMod) folderPermissionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"folder_permissions\".\"created_by\"=?", o.ID),
	)

	return FolderPermissions(queryMods...)
}

// FolderPermissions retrieves all the folder_permission's FolderPermissions with an executor.
func (o *User) FolderPermissions(mods ...qm.QueryMod) folderPermissionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"folder_permissions\".\"user_id\"=?", o.ID),
	)

	return FolderPermissions(queryMods...)
}

// CreatedByFolders retrieves all the folder's Folders with an executor via created_by column.
func (o *User) CreatedByFolders(mods ...qm.QueryMod) folderQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"folders\".\"created_by\"=?", o.ID),
	)

	return Folders(queryMods...)
}

// UpdatedByFolders retrieves all the folder's Folders with an executor via updated_by column.
func (o *User) UpdatedByFolders(mods ...qm.QueryMod) folderQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"folders\".\"updated_by\"=?", o.ID),
	)

	return Folders(queryMods...)
}

// PortalMemberships retrieves all the portal_membership's PortalMemberships with an executor.
func (o *User) PortalMemberships(mods ...qm.QueryMod) portalMembershipQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portal_memberships\".\"user_id\"=?", o.ID),
	)

	return PortalMemberships(queryMods...)
}

// CreatedByPortalRoles retrieves all the portal_role's PortalRoles with an executor via created_by column.
func (o *User) CreatedByPortalRoles(mods ...qm.QueryMod) portalRoleQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portal_roles\".\"created_by\"=?", o.ID),
	)

	return PortalRoles(queryMods...)
}

// UpdatedByPortalRoles retrieves all the portal_role's PortalRoles with an executor via updated_by column.
func (o *User) UpdatedByPortalRoles(mods ...qm.QueryMod) portalRoleQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portal_roles\".\"updated_by\"=?", o.ID),
	)

	return PortalRoles(queryMods...)
}

// CreatedByPortals retrieves all the portal's Portals with an executor via created_by column.
func (o *User) CreatedByPortals(mods ...qm.QueryMod) portalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portals\".\"created_by\"=?", o.ID),
	)

	return Portals(queryMods...)
}

// UpdatedByPortals retrieves all the portal's Portals with an executor via updated_by column.
func (o *User) UpdatedByPortals(mods ...qm.QueryMod) portalQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portals\".\"updated_by\"=?", o.ID),
	)

	return Portals(queryMods...)
}

// LoadGlobalRole allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (userL) LoadGlobalRole(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.GlobalRoleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}

			args[obj.GlobalRoleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`global_roles`),
		qm.WhereIn(`global_roles.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load GlobalRole")
	}

	var resultSlice []*GlobalRole
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice GlobalRole")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for global_roles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for global_roles")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.GlobalRole = foreign
		if foreign.R == nil {
			foreign.R = &globalRoleR{}
		}
		foreign.R.Users = append(foreign.R.Users, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.GlobalRoleID == foreign.ID {
				local.R.GlobalRole = foreign
				if foreign.R == nil {
					foreign.R = &globalRoleR{}
				}
				foreign.R.Users = append(foreign.R.Users, local)
				break
			}
		}
	}

	return nil
}

// LoadCreatedByDocumentFiles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByDocumentFiles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`document_files`),
		qm.WhereIn(`document_files.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load document_files")
	}

	var resultSlice []*DocumentFile
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice document_files")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on document_files")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for document_files")
	}

	if singular {
		object.R.CreatedByDocumentFiles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentFileR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByDocumentFiles = append(local.R.CreatedByDocumentFiles, foreign)
				if foreign.R == nil {
					foreign.R = &documentFileR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByDocumentLinks allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByDocumentLinks(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`document_links`),
		qm.WhereIn(`document_links.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load document_links")
	}

	var resultSlice []*DocumentLink
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice document_links")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on document_links")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for document_links")
	}

	if singular {
		object.R.CreatedByDocumentLinks = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentLinkR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByDocumentLinks = append(local.R.CreatedByDocumentLinks, foreign)
				if foreign.R == nil {
					foreign.R = &documentLinkR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByDocumentVersions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByDocumentVersions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`document_versions`),
		qm.WhereIn(`document_versions.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load document_versions")
	}

	var resultSlice []*DocumentVersion
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice document_versions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on document_versions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for document_versions")
	}

	if singular {
		object.R.CreatedByDocumentVersions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentVersionR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByDocumentVersions = append(local.R.CreatedByDocumentVersions, foreign)
				if foreign.R == nil {
					foreign.R = &documentVersionR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByDocuments allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByDocuments(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documents`),
		qm.WhereIn(`documents.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documents")
	}

	var resultSlice []*Document
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documents")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documents")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documents")
	}

	if singular {
		object.R.CreatedByDocuments = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByDocuments = append(local.R.CreatedByDocuments, foreign)
				if foreign.R == nil {
					foreign.R = &documentR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadOwnerDocuments allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadOwnerDocuments(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documents`),
		qm.WhereIn(`documents.owner_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documents")
	}

	var resultSlice []*Document
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documents")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documents")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documents")
	}

	if singular {
		object.R.OwnerDocuments = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentR{}
			}
			foreign.R.Owner = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.OwnerID) {
				local.R.OwnerDocuments = append(local.R.OwnerDocuments, foreign)
				if foreign.R == nil {
					foreign.R = &documentR{}
				}
				foreign.R.Owner = local
				break
			}
		}
	}

	return nil
}

// LoadUpdatedByDocuments allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadUpdatedByDocuments(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`documents`),
		qm.WhereIn(`documents.updated_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load documents")
	}

	var resultSlice []*Document
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice documents")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on documents")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for documents")
	}

	if singular {
		object.R.UpdatedByDocuments = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentR{}
			}
			foreign.R.UpdatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.UpdatedBy) {
				local.R.UpdatedByDocuments = append(local.R.UpdatedByDocuments, foreign)
				if foreign.R == nil {
					foreign.R = &documentR{}
				}
				foreign.R.UpdatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadUploadedByFiles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadUploadedByFiles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`files`),
		qm.WhereIn(`files.uploaded_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load files")
	}

	var resultSlice []*File
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice files")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on files")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for files")
	}

	if singular {
		object.R.UploadedByFiles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &fileR{}
			}
			foreign.R.UploadedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.UploadedBy {
				local.R.UploadedByFiles = append(local.R.UploadedByFiles, foreign)
				if foreign.R == nil {
					foreign.R = &fileR{}
				}
				foreign.R.UploadedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByFolderPermissions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByFolderPermissions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`folder_permissions`),
		qm.WhereIn(`folder_permissions.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load folder_permissions")
	}

	var resultSlice []*FolderPermission
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice folder_permissions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on folder_permissions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for folder_permissions")
	}

	if singular {
		object.R.CreatedByFolderPermissions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &folderPermissionR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByFolderPermissions = append(local.R.CreatedByFolderPermissions, foreign)
				if foreign.R == nil {
					foreign.R = &folderPermissionR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadFolderPermissions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadFolderPermissions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`folder_permissions`),
		qm.WhereIn(`folder_permissions.user_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load folder_permissions")
	}

	var resultSlice []*FolderPermission
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice folder_permissions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on folder_permissions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for folder_permissions")
	}

	if singular {
		object.R.FolderPermissions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &folderPermissionR{}
			}
			foreign.R.User = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.UserID) {
				local.R.FolderPermissions = append(local.R.FolderPermissions, foreign)
				if foreign.R == nil {
					foreign.R = &folderPermissionR{}
				}
				foreign.R.User = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByFolders allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByFolders(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`folders`),
		qm.WhereIn(`folders.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load folders")
	}

	var resultSlice []*Folder
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice folders")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on folders")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for folders")
	}

	if singular {
		object.R.CreatedByFolders = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &folderR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByFolders = append(local.R.CreatedByFolders, foreign)
				if foreign.R == nil {
					foreign.R = &folderR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadUpdatedByFolders allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadUpdatedByFolders(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`folders`),
		qm.WhereIn(`folders.updated_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load folders")
	}

	var resultSlice []*Folder
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice folders")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on folders")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for folders")
	}

	if singular {
		object.R.UpdatedByFolders = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &folderR{}
			}
			foreign.R.UpdatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.UpdatedBy) {
				local.R.UpdatedByFolders = append(local.R.UpdatedByFolders, foreign)
				if foreign.R == nil {
					foreign.R = &folderR{}
				}
				foreign.R.UpdatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadPortalMemberships allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadPortalMemberships(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_memberships`),
		qm.WhereIn(`portal_memberships.user_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portal_memberships")
	}

	var resultSlice []*PortalMembership
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portal_memberships")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portal_memberships")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_memberships")
	}

	if singular {
		object.R.PortalMemberships = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalMembershipR{}
			}
			foreign.R.User = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.UserID {
				local.R.PortalMemberships = append(local.R.PortalMemberships, foreign)
				if foreign.R == nil {
					foreign.R = &portalMembershipR{}
				}
				foreign.R.User = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByPortalRoles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByPortalRoles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_roles`),
		qm.WhereIn(`portal_roles.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portal_roles")
	}

	var resultSlice []*PortalRole
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portal_roles")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portal_roles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_roles")
	}

	if singular {
		object.R.CreatedByPortalRoles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalRoleR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByPortalRoles = append(local.R.CreatedByPortalRoles, foreign)
				if foreign.R == nil {
					foreign.R = &portalRoleR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadUpdatedByPortalRoles allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadUpdatedByPortalRoles(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_roles`),
		qm.WhereIn(`portal_roles.updated_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portal_roles")
	}

	var resultSlice []*PortalRole
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portal_roles")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portal_roles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_roles")
	}

	if singular {
		object.R.UpdatedByPortalRoles = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalRoleR{}
			}
			foreign.R.UpdatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.UpdatedBy) {
				local.R.UpdatedByPortalRoles = append(local.R.UpdatedByPortalRoles, foreign)
				if foreign.R == nil {
					foreign.R = &portalRoleR{}
				}
				foreign.R.UpdatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadCreatedByPortals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadCreatedByPortals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portals`),
		qm.WhereIn(`portals.created_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portals")
	}

	var resultSlice []*Portal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portals")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portals")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portals")
	}

	if singular {
		object.R.CreatedByPortals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalR{}
			}
			foreign.R.CreatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.CreatedBy {
				local.R.CreatedByPortals = append(local.R.CreatedByPortals, foreign)
				if foreign.R == nil {
					foreign.R = &portalR{}
				}
				foreign.R.CreatedByUser = local
				break
			}
		}
	}

	return nil
}

// LoadUpdatedByPortals allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (userL) LoadUpdatedByPortals(ctx context.Context, e boil.ContextExecutor, singular bool, maybeUser any, mods queries.Applicator) error {
	var slice []*User
	var object *User

	if singular {
		var ok bool
		object, ok = maybeUser.(*User)
		if !ok {
			object = new(User)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeUser))
			}
		}
	} else {
		s, ok := maybeUser.(*[]*User)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeUser)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeUser))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &userR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &userR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portals`),
		qm.WhereIn(`portals.updated_by in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portals")
	}

	var resultSlice []*Portal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portals")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portals")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portals")
	}

	if singular {
		object.R.UpdatedByPortals = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalR{}
			}
			foreign.R.UpdatedByUser = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.UpdatedBy) {
				local.R.UpdatedByPortals = append(local.R.UpdatedByPortals, foreign)
				if foreign.R == nil {
					foreign.R = &portalR{}
				}
				foreign.R.UpdatedByUser = local
				break
			}
		}
	}

	return nil
}

// SetGlobalRole of the user to the related item.
// Sets o.R.GlobalRole to related.
// Adds o to related.R.Users.
func (o *User) SetGlobalRole(ctx context.Context, exec boil.ContextExecutor, insert bool, related *GlobalRole) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"users\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"global_role_id"}),
		strmangle.WhereClause("\"", "\"", 2, userPrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.GlobalRoleID = related.ID
	if o.R == nil {
		o.R = &userR{
			GlobalRole: related,
		}
	} else {
		o.R.GlobalRole = related
	}

	if related.R == nil {
		related.R = &globalRoleR{
			Users: UserSlice{o},
		}
	} else {
		related.R.Users = append(related.R.Users, o)
	}

	return nil
}

// AddCreatedByDocumentFiles adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByDocumentFiles.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByDocumentFiles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentFile) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"document_files\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, documentFilePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByDocumentFiles: related,
		}
	} else {
		o.R.CreatedByDocumentFiles = append(o.R.CreatedByDocumentFiles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentFileR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddCreatedByDocumentLinks adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByDocumentLinks.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByDocumentLinks(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentLink) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"document_links\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, documentLinkPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByDocumentLinks: related,
		}
	} else {
		o.R.CreatedByDocumentLinks = append(o.R.CreatedByDocumentLinks, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentLinkR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddCreatedByDocumentVersions adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByDocumentVersions.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByDocumentVersions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentVersion) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"document_versions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, documentVersionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByDocumentVersions: related,
		}
	} else {
		o.R.CreatedByDocumentVersions = append(o.R.CreatedByDocumentVersions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentVersionR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddCreatedByDocuments adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByDocuments.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByDocuments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Document) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"documents\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, documentPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByDocuments: related,
		}
	} else {
		o.R.CreatedByDocuments = append(o.R.CreatedByDocuments, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddOwnerDocuments adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.OwnerDocuments.
// Sets related.R.Owner appropriately.
func (o *User) AddOwnerDocuments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Document) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.OwnerID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"documents\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"owner_id"}),
				strmangle.WhereClause("\"", "\"", 2, documentPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.OwnerID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &userR{
			OwnerDocuments: related,
		}
	} else {
		o.R.OwnerDocuments = append(o.R.OwnerDocuments, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentR{
				Owner: o,
			}
		} else {
			rel.R.Owner = o
		}
	}
	return nil
}

// SetOwnerDocuments removes all previously related items of the
// user replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Owner's OwnerDocuments accordingly.
// Replaces o.R.OwnerDocuments with related.
// Sets related.R.Owner's OwnerDocuments accordingly.
func (o *User) SetOwnerDocuments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Document) error {
	query := "update \"documents\" set \"owner_id\" = null where \"owner_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.OwnerDocuments {
			queries.SetScanner(&rel.OwnerID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Owner = nil
		}
		o.R.OwnerDocuments = nil
	}

	return o.AddOwnerDocuments(ctx, exec, insert, related...)
}

// RemoveOwnerDocuments relationships from objects passed in.
// Removes related items from R.OwnerDocuments (uses pointer comparison, removal does not keep order)
// Sets related.R.Owner.
func (o *User) RemoveOwnerDocuments(ctx context.Context, exec boil.ContextExecutor, related ...*Document) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.OwnerID, nil)
		if rel.R != nil {
			rel.R.Owner = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("owner_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.OwnerDocuments {
			if rel != ri {
				continue
			}

			ln := len(o.R.OwnerDocuments)
			if ln > 1 && i < ln-1 {
				o.R.OwnerDocuments[i] = o.R.OwnerDocuments[ln-1]
			}
			o.R.OwnerDocuments = o.R.OwnerDocuments[:ln-1]
			break
		}
	}

	return nil
}

// AddUpdatedByDocuments adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.UpdatedByDocuments.
// Sets related.R.UpdatedByUser appropriately.
func (o *User) AddUpdatedByDocuments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Document) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UpdatedBy, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"documents\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"updated_by"}),
				strmangle.WhereClause("\"", "\"", 2, documentPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UpdatedBy, o.ID)
		}
	}

	if o.R == nil {
		o.R = &userR{
			UpdatedByDocuments: related,
		}
	} else {
		o.R.UpdatedByDocuments = append(o.R.UpdatedByDocuments, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentR{
				UpdatedByUser: o,
			}
		} else {
			rel.R.UpdatedByUser = o
		}
	}
	return nil
}

// SetUpdatedByDocuments removes all previously related items of the
// user replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UpdatedByUser's UpdatedByDocuments accordingly.
// Replaces o.R.UpdatedByDocuments with related.
// Sets related.R.UpdatedByUser's UpdatedByDocuments accordingly.
func (o *User) SetUpdatedByDocuments(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Document) error {
	query := "update \"documents\" set \"updated_by\" = null where \"updated_by\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UpdatedByDocuments {
			queries.SetScanner(&rel.UpdatedBy, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UpdatedByUser = nil
		}
		o.R.UpdatedByDocuments = nil
	}

	return o.AddUpdatedByDocuments(ctx, exec, insert, related...)
}

// RemoveUpdatedByDocuments relationships from objects passed in.
// Removes related items from R.UpdatedByDocuments (uses pointer comparison, removal does not keep order)
// Sets related.R.UpdatedByUser.
func (o *User) RemoveUpdatedByDocuments(ctx context.Context, exec boil.ContextExecutor, related ...*Document) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UpdatedBy, nil)
		if rel.R != nil {
			rel.R.UpdatedByUser = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("updated_by")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UpdatedByDocuments {
			if rel != ri {
				continue
			}

			ln := len(o.R.UpdatedByDocuments)
			if ln > 1 && i < ln-1 {
				o.R.UpdatedByDocuments[i] = o.R.UpdatedByDocuments[ln-1]
			}
			o.R.UpdatedByDocuments = o.R.UpdatedByDocuments[:ln-1]
			break
		}
	}

	return nil
}

// AddUploadedByFiles adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.UploadedByFiles.
// Sets related.R.UploadedByUser appropriately.
func (o *User) AddUploadedByFiles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*File) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.UploadedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"files\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"uploaded_by"}),
				strmangle.WhereClause("\"", "\"", 2, filePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.UploadedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			UploadedByFiles: related,
		}
	} else {
		o.R.UploadedByFiles = append(o.R.UploadedByFiles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &fileR{
				UploadedByUser: o,
			}
		} else {
			rel.R.UploadedByUser = o
		}
	}
	return nil
}

// AddCreatedByFolderPermissions adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByFolderPermissions.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByFolderPermissions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FolderPermission) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"folder_permissions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, folderPermissionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByFolderPermissions: related,
		}
	} else {
		o.R.CreatedByFolderPermissions = append(o.R.CreatedByFolderPermissions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &folderPermissionR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddFolderPermissions adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.FolderPermissions.
// Sets related.R.User appropriately.
func (o *User) AddFolderPermissions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FolderPermission) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UserID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"folder_permissions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
				strmangle.WhereClause("\"", "\"", 2, folderPermissionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UserID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &userR{
			FolderPermissions: related,
		}
	} else {
		o.R.FolderPermissions = append(o.R.FolderPermissions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &folderPermissionR{
				User: o,
			}
		} else {
			rel.R.User = o
		}
	}
	return nil
}

// SetFolderPermissions removes all previously related items of the
// user replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.User's FolderPermissions accordingly.
// Replaces o.R.FolderPermissions with related.
// Sets related.R.User's FolderPermissions accordingly.
func (o *User) SetFolderPermissions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FolderPermission) error {
	query := "update \"folder_permissions\" set \"user_id\" = null where \"user_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.FolderPermissions {
			queries.SetScanner(&rel.UserID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.User = nil
		}
		o.R.FolderPermissions = nil
	}

	return o.AddFolderPermissions(ctx, exec, insert, related...)
}

// RemoveFolderPermissions relationships from objects passed in.
// Removes related items from R.FolderPermissions (uses pointer comparison, removal does not keep order)
// Sets related.R.User.
func (o *User) RemoveFolderPermissions(ctx context.Context, exec boil.ContextExecutor, related ...*FolderPermission) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UserID, nil)
		if rel.R != nil {
			rel.R.User = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("user_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.FolderPermissions {
			if rel != ri {
				continue
			}

			ln := len(o.R.FolderPermissions)
			if ln > 1 && i < ln-1 {
				o.R.FolderPermissions[i] = o.R.FolderPermissions[ln-1]
			}
			o.R.FolderPermissions = o.R.FolderPermissions[:ln-1]
			break
		}
	}

	return nil
}

// AddCreatedByFolders adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByFolders.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByFolders(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Folder) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"folders\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, folderPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByFolders: related,
		}
	} else {
		o.R.CreatedByFolders = append(o.R.CreatedByFolders, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &folderR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddUpdatedByFolders adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.UpdatedByFolders.
// Sets related.R.UpdatedByUser appropriately.
func (o *User) AddUpdatedByFolders(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Folder) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UpdatedBy, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"folders\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"updated_by"}),
				strmangle.WhereClause("\"", "\"", 2, folderPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UpdatedBy, o.ID)
		}
	}

	if o.R == nil {
		o.R = &userR{
			UpdatedByFolders: related,
		}
	} else {
		o.R.UpdatedByFolders = append(o.R.UpdatedByFolders, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &folderR{
				UpdatedByUser: o,
			}
		} else {
			rel.R.UpdatedByUser = o
		}
	}
	return nil
}

// SetUpdatedByFolders removes all previously related items of the
// user replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UpdatedByUser's UpdatedByFolders accordingly.
// Replaces o.R.UpdatedByFolders with related.
// Sets related.R.UpdatedByUser's UpdatedByFolders accordingly.
func (o *User) SetUpdatedByFolders(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Folder) error {
	query := "update \"folders\" set \"updated_by\" = null where \"updated_by\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UpdatedByFolders {
			queries.SetScanner(&rel.UpdatedBy, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UpdatedByUser = nil
		}
		o.R.UpdatedByFolders = nil
	}

	return o.AddUpdatedByFolders(ctx, exec, insert, related...)
}

// RemoveUpdatedByFolders relationships from objects passed in.
// Removes related items from R.UpdatedByFolders (uses pointer comparison, removal does not keep order)
// Sets related.R.UpdatedByUser.
func (o *User) RemoveUpdatedByFolders(ctx context.Context, exec boil.ContextExecutor, related ...*Folder) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UpdatedBy, nil)
		if rel.R != nil {
			rel.R.UpdatedByUser = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("updated_by")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UpdatedByFolders {
			if rel != ri {
				continue
			}

			ln := len(o.R.UpdatedByFolders)
			if ln > 1 && i < ln-1 {
				o.R.UpdatedByFolders[i] = o.R.UpdatedByFolders[ln-1]
			}
			o.R.UpdatedByFolders = o.R.UpdatedByFolders[:ln-1]
			break
		}
	}

	return nil
}

// AddPortalMemberships adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.PortalMemberships.
// Sets related.R.User appropriately.
func (o *User) AddPortalMemberships(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PortalMembership) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.UserID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portal_memberships\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
				strmangle.WhereClause("\"", "\"", 2, portalMembershipPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.UserID = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			PortalMemberships: related,
		}
	} else {
		o.R.PortalMemberships = append(o.R.PortalMemberships, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalMembershipR{
				User: o,
			}
		} else {
			rel.R.User = o
		}
	}
	return nil
}

// AddCreatedByPortalRoles adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByPortalRoles.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByPortalRoles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PortalRole) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portal_roles\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, portalRolePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByPortalRoles: related,
		}
	} else {
		o.R.CreatedByPortalRoles = append(o.R.CreatedByPortalRoles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalRoleR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddUpdatedByPortalRoles adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.UpdatedByPortalRoles.
// Sets related.R.UpdatedByUser appropriately.
func (o *User) AddUpdatedByPortalRoles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PortalRole) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UpdatedBy, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portal_roles\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"updated_by"}),
				strmangle.WhereClause("\"", "\"", 2, portalRolePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UpdatedBy, o.ID)
		}
	}

	if o.R == nil {
		o.R = &userR{
			UpdatedByPortalRoles: related,
		}
	} else {
		o.R.UpdatedByPortalRoles = append(o.R.UpdatedByPortalRoles, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalRoleR{
				UpdatedByUser: o,
			}
		} else {
			rel.R.UpdatedByUser = o
		}
	}
	return nil
}

// SetUpdatedByPortalRoles removes all previously related items of the
// user replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UpdatedByUser's UpdatedByPortalRoles accordingly.
// Replaces o.R.UpdatedByPortalRoles with related.
// Sets related.R.UpdatedByUser's UpdatedByPortalRoles accordingly.
func (o *User) SetUpdatedByPortalRoles(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PortalRole) error {
	query := "update \"portal_roles\" set \"updated_by\" = null where \"updated_by\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UpdatedByPortalRoles {
			queries.SetScanner(&rel.UpdatedBy, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UpdatedByUser = nil
		}
		o.R.UpdatedByPortalRoles = nil
	}

	return o.AddUpdatedByPortalRoles(ctx, exec, insert, related...)
}

// RemoveUpdatedByPortalRoles relationships from objects passed in.
// Removes related items from R.UpdatedByPortalRoles (uses pointer comparison, removal does not keep order)
// Sets related.R.UpdatedByUser.
func (o *User) RemoveUpdatedByPortalRoles(ctx context.Context, exec boil.ContextExecutor, related ...*PortalRole) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UpdatedBy, nil)
		if rel.R != nil {
			rel.R.UpdatedByUser = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("updated_by")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UpdatedByPortalRoles {
			if rel != ri {
				continue
			}

			ln := len(o.R.UpdatedByPortalRoles)
			if ln > 1 && i < ln-1 {
				o.R.UpdatedByPortalRoles[i] = o.R.UpdatedByPortalRoles[ln-1]
			}
			o.R.UpdatedByPortalRoles = o.R.UpdatedByPortalRoles[:ln-1]
			break
		}
	}

	return nil
}

// AddCreatedByPortals adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.CreatedByPortals.
// Sets related.R.CreatedByUser appropriately.
func (o *User) AddCreatedByPortals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Portal) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.CreatedBy = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portals\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
				strmangle.WhereClause("\"", "\"", 2, portalPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.CreatedBy = o.ID
		}
	}

	if o.R == nil {
		o.R = &userR{
			CreatedByPortals: related,
		}
	} else {
		o.R.CreatedByPortals = append(o.R.CreatedByPortals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalR{
				CreatedByUser: o,
			}
		} else {
			rel.R.CreatedByUser = o
		}
	}
	return nil
}

// AddUpdatedByPortals adds the given related objects to the existing relationships
// of the user, optionally inserting them as new records.
// Appends related to o.R.UpdatedByPortals.
// Sets related.R.UpdatedByUser appropriately.
func (o *User) AddUpdatedByPortals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Portal) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.UpdatedBy, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portals\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"updated_by"}),
				strmangle.WhereClause("\"", "\"", 2, portalPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.UpdatedBy, o.ID)
		}
	}

	if o.R == nil {
		o.R = &userR{
			UpdatedByPortals: related,
		}
	} else {
		o.R.UpdatedByPortals = append(o.R.UpdatedByPortals, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalR{
				UpdatedByUser: o,
			}
		} else {
			rel.R.UpdatedByUser = o
		}
	}
	return nil
}

// SetUpdatedByPortals removes all previously related items of the
// user replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.UpdatedByUser's UpdatedByPortals accordingly.
// Replaces o.R.UpdatedByPortals with related.
// Sets related.R.UpdatedByUser's UpdatedByPortals accordingly.
func (o *User) SetUpdatedByPortals(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Portal) error {
	query := "update \"portals\" set \"updated_by\" = null where \"updated_by\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.UpdatedByPortals {
			queries.SetScanner(&rel.UpdatedBy, nil)
			if rel.R == nil {
				continue
			}

			rel.R.UpdatedByUser = nil
		}
		o.R.UpdatedByPortals = nil
	}

	return o.AddUpdatedByPortals(ctx, exec, insert, related...)
}

// RemoveUpdatedByPortals relationships from objects passed in.
// Removes related items from R.UpdatedByPortals (uses pointer comparison, removal does not keep order)
// Sets related.R.UpdatedByUser.
func (o *User) RemoveUpdatedByPortals(ctx context.Context, exec boil.ContextExecutor, related ...*Portal) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.UpdatedBy, nil)
		if rel.R != nil {
			rel.R.UpdatedByUser = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("updated_by")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.UpdatedByPortals {
			if rel != ri {
				continue
			}

			ln := len(o.R.UpdatedByPortals)
			if ln > 1 && i < ln-1 {
				o.R.UpdatedByPortals[i] = o.R.UpdatedByPortals[ln-1]
			}
			o.R.UpdatedByPortals = o.R.UpdatedByPortals[:ln-1]
			break
		}
	}

	return nil
}

// Users retrieves all the records using an executor.
func Users(mods ...qm.QueryMod) userQuery {
	mods = append(mods, qm.From("\"users\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"users\".*"})
	}

	return userQuery{q}
}

// FindUser retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindUser(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*User, error) {
	userObj := &User{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"users\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, userObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from users")
	}

	return userObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *User) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no users provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(userColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	userInsertCacheMut.RLock()
	cache, cached := userInsertCache[key]
	userInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			userAllColumns,
			userColumnsWithDefault,
			userColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, userGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(userType, userMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(userType, userMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"users\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"users\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into users")
	}

	if !cached {
		userInsertCacheMut.Lock()
		userInsertCache[key] = cache
		userInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the User.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *User) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	userUpdateCacheMut.RLock()
	cache, cached := userUpdateCache[key]
	userUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			userAllColumns,
			userPrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, userGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update users, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"users\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, userPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(userType, userMapping, append(wl, userPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update users row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for users")
	}

	if !cached {
		userUpdateCacheMut.Lock()
		userUpdateCache[key] = cache
		userUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q userQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for users")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for users")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o UserSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"users\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, userPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in user slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all user")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *User) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no users provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	nzDefaults := queries.NonZeroDefaultSet(userColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	userUpsertCacheMut.RLock()
	cache, cached := userUpsertCache[key]
	userUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			userAllColumns,
			userColumnsWithDefault,
			userColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			userAllColumns,
			userPrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, userGeneratedColumns)
		update = strmangle.SetComplement(update, userGeneratedColumns)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert users, could not build update column list")
		}

		ret := strmangle.SetComplement(userAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(userPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert users, could not build conflict column list")
			}

			conflict = make([]string, len(userPrimaryKeyColumns))
			copy(conflict, userPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"users\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(userType, userMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(userType, userMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert users")
	}

	if !cached {
		userUpsertCacheMut.Lock()
		userUpsertCache[key] = cache
		userUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single User record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *User) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no User provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), userPrimaryKeyMapping)
	sql := "DELETE FROM \"users\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from users")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for users")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q userQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no userQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from users")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for users")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o UserSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"users\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from user slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for users")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *User) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindUser(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *UserSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := UserSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), userPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"users\".* FROM \"users\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, userPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in UserSlice")
	}

	*o = slice

	return nil
}

// UserExists checks if the User row exists.
func UserExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"users\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if users exists")
	}

	return exists, nil
}

// Exists checks if the User row exists.
func (o *User) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return UserExists(ctx, exec, o.ID)
}
