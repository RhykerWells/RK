// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// PortalRole is an object representing the database table.
type PortalRole struct {
	ID          int64       `boil:"id" json:"id" toml:"id" yaml:"id"`
	PortalID    int64       `boil:"portal_id" json:"portal_id" toml:"portal_id" yaml:"portal_id"`
	Name        string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	Description null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	CreatedBy   int64       `boil:"created_by" json:"created_by" toml:"created_by" yaml:"created_by"`
	UpdatedBy   null.Int64  `boil:"updated_by" json:"updated_by,omitempty" toml:"updated_by" yaml:"updated_by,omitempty"`
	CreatedAt   time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt   time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *portalRoleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L portalRoleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PortalRoleColumns = struct {
	ID          string
	PortalID    string
	Name        string
	Description string
	CreatedBy   string
	UpdatedBy   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	PortalID:    "portal_id",
	Name:        "name",
	Description: "description",
	CreatedBy:   "created_by",
	UpdatedBy:   "updated_by",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var PortalRoleTableColumns = struct {
	ID          string
	PortalID    string
	Name        string
	Description string
	CreatedBy   string
	UpdatedBy   string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "portal_roles.id",
	PortalID:    "portal_roles.portal_id",
	Name:        "portal_roles.name",
	Description: "portal_roles.description",
	CreatedBy:   "portal_roles.created_by",
	UpdatedBy:   "portal_roles.updated_by",
	CreatedAt:   "portal_roles.created_at",
	UpdatedAt:   "portal_roles.updated_at",
}

// Generated where

var PortalRoleWhere = struct {
	ID          whereHelperint64
	PortalID    whereHelperint64
	Name        whereHelperstring
	Description whereHelpernull_String
	CreatedBy   whereHelperint64
	UpdatedBy   whereHelpernull_Int64
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperint64{field: "\"portal_roles\".\"id\""},
	PortalID:    whereHelperint64{field: "\"portal_roles\".\"portal_id\""},
	Name:        whereHelperstring{field: "\"portal_roles\".\"name\""},
	Description: whereHelpernull_String{field: "\"portal_roles\".\"description\""},
	CreatedBy:   whereHelperint64{field: "\"portal_roles\".\"created_by\""},
	UpdatedBy:   whereHelpernull_Int64{field: "\"portal_roles\".\"updated_by\""},
	CreatedAt:   whereHelpertime_Time{field: "\"portal_roles\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"portal_roles\".\"updated_at\""},
}

// PortalRoleRels is where relationship names are stored.
var PortalRoleRels = struct {
	CreatedByUser               string
	Portal                      string
	UpdatedByUser               string
	DocumentPermissionOverrides string
	FolderPermissionOverrides   string
	PortalMemberships           string
	PortalRolePermissions       string
}{
	CreatedByUser:               "CreatedByUser",
	Portal:                      "Portal",
	UpdatedByUser:               "UpdatedByUser",
	DocumentPermissionOverrides: "DocumentPermissionOverrides",
	FolderPermissionOverrides:   "FolderPermissionOverrides",
	PortalMemberships:           "PortalMemberships",
	PortalRolePermissions:       "PortalRolePermissions",
}

// portalRoleR is where relationships are stored.
type portalRoleR struct {
	CreatedByUser               *User                           `boil:"CreatedByUser" json:"CreatedByUser" toml:"CreatedByUser" yaml:"CreatedByUser"`
	Portal                      *Portal                         `boil:"Portal" json:"Portal" toml:"Portal" yaml:"Portal"`
	UpdatedByUser               *User                           `boil:"UpdatedByUser" json:"UpdatedByUser" toml:"UpdatedByUser" yaml:"UpdatedByUser"`
	DocumentPermissionOverrides DocumentPermissionOverrideSlice `boil:"DocumentPermissionOverrides" json:"DocumentPermissionOverrides" toml:"DocumentPermissionOverrides" yaml:"DocumentPermissionOverrides"`
	FolderPermissionOverrides   FolderPermissionOverrideSlice   `boil:"FolderPermissionOverrides" json:"FolderPermissionOverrides" toml:"FolderPermissionOverrides" yaml:"FolderPermissionOverrides"`
	PortalMemberships           PortalMembershipSlice           `boil:"PortalMemberships" json:"PortalMemberships" toml:"PortalMemberships" yaml:"PortalMemberships"`
	PortalRolePermissions       PortalRolePermissionSlice       `boil:"PortalRolePermissions" json:"PortalRolePermissions" toml:"PortalRolePermissions" yaml:"PortalRolePermissions"`
}

// NewStruct creates a new relationship struct
func (*portalRoleR) NewStruct() *portalRoleR {
	return &portalRoleR{}
}

func (o *PortalRole) GetCreatedByUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetCreatedByUser()
}

func (r *portalRoleR) GetCreatedByUser() *User {
	if r == nil {
		return nil
	}

	return r.CreatedByUser
}

func (o *PortalRole) GetPortal() *Portal {
	if o == nil {
		return nil
	}

	return o.R.GetPortal()
}

func (r *portalRoleR) GetPortal() *Portal {
	if r == nil {
		return nil
	}

	return r.Portal
}

func (o *PortalRole) GetUpdatedByUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUpdatedByUser()
}

func (r *portalRoleR) GetUpdatedByUser() *User {
	if r == nil {
		return nil
	}

	return r.UpdatedByUser
}

func (o *PortalRole) GetDocumentPermissionOverrides() DocumentPermissionOverrideSlice {
	if o == nil {
		return nil
	}

	return o.R.GetDocumentPermissionOverrides()
}

func (r *portalRoleR) GetDocumentPermissionOverrides() DocumentPermissionOverrideSlice {
	if r == nil {
		return nil
	}

	return r.DocumentPermissionOverrides
}

func (o *PortalRole) GetFolderPermissionOverrides() FolderPermissionOverrideSlice {
	if o == nil {
		return nil
	}

	return o.R.GetFolderPermissionOverrides()
}

func (r *portalRoleR) GetFolderPermissionOverrides() FolderPermissionOverrideSlice {
	if r == nil {
		return nil
	}

	return r.FolderPermissionOverrides
}

func (o *PortalRole) GetPortalMemberships() PortalMembershipSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPortalMemberships()
}

func (r *portalRoleR) GetPortalMemberships() PortalMembershipSlice {
	if r == nil {
		return nil
	}

	return r.PortalMemberships
}

func (o *PortalRole) GetPortalRolePermissions() PortalRolePermissionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetPortalRolePermissions()
}

func (r *portalRoleR) GetPortalRolePermissions() PortalRolePermissionSlice {
	if r == nil {
		return nil
	}

	return r.PortalRolePermissions
}

// portalRoleL is where Load methods for each relationship are stored.
type portalRoleL struct{}

var (
	portalRoleAllColumns            = []string{"id", "portal_id", "name", "description", "created_by", "updated_by", "created_at", "updated_at"}
	portalRoleColumnsWithoutDefault = []string{"portal_id", "name", "created_by"}
	portalRoleColumnsWithDefault    = []string{"id", "description", "updated_by", "created_at", "updated_at"}
	portalRolePrimaryKeyColumns     = []string{"id"}
	portalRoleGeneratedColumns      = []string{"id"}
)

type (
	// PortalRoleSlice is an alias for a slice of pointers to PortalRole.
	// This should almost always be used instead of []PortalRole.
	PortalRoleSlice []*PortalRole

	portalRoleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	portalRoleType                 = reflect.TypeOf(&PortalRole{})
	portalRoleMapping              = queries.MakeStructMapping(portalRoleType)
	portalRolePrimaryKeyMapping, _ = queries.BindMapping(portalRoleType, portalRoleMapping, portalRolePrimaryKeyColumns)
	portalRoleInsertCacheMut       sync.RWMutex
	portalRoleInsertCache          = make(map[string]insertCache)
	portalRoleUpdateCacheMut       sync.RWMutex
	portalRoleUpdateCache          = make(map[string]updateCache)
	portalRoleUpsertCacheMut       sync.RWMutex
	portalRoleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single portalRole record from the query.
func (q portalRoleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PortalRole, error) {
	o := &PortalRole{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for portal_roles")
	}

	return o, nil
}

// All returns all PortalRole records from the query.
func (q portalRoleQuery) All(ctx context.Context, exec boil.ContextExecutor) (PortalRoleSlice, error) {
	var o []*PortalRole

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PortalRole slice")
	}

	return o, nil
}

// Count returns the count of all PortalRole records in the query.
func (q portalRoleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count portal_roles rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q portalRoleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if portal_roles exists")
	}

	return count > 0, nil
}

// CreatedByUser pointed to by the foreign key.
func (o *PortalRole) CreatedByUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.CreatedBy),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// Portal pointed to by the foreign key.
func (o *PortalRole) Portal(mods ...qm.QueryMod) portalQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PortalID),
	}

	queryMods = append(queryMods, mods...)

	return Portals(queryMods...)
}

// UpdatedByUser pointed to by the foreign key.
func (o *PortalRole) UpdatedByUser(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UpdatedBy),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// DocumentPermissionOverrides retrieves all the document_permission_override's DocumentPermissionOverrides with an executor.
func (o *PortalRole) DocumentPermissionOverrides(mods ...qm.QueryMod) documentPermissionOverrideQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"document_permission_overrides\".\"portal_role_id\"=?", o.ID),
	)

	return DocumentPermissionOverrides(queryMods...)
}

// FolderPermissionOverrides retrieves all the folder_permission_override's FolderPermissionOverrides with an executor.
func (o *PortalRole) FolderPermissionOverrides(mods ...qm.QueryMod) folderPermissionOverrideQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"folder_permission_overrides\".\"portal_role_id\"=?", o.ID),
	)

	return FolderPermissionOverrides(queryMods...)
}

// PortalMemberships retrieves all the portal_membership's PortalMemberships with an executor.
func (o *PortalRole) PortalMemberships(mods ...qm.QueryMod) portalMembershipQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portal_memberships\".\"portal_role_id\"=?", o.ID),
	)

	return PortalMemberships(queryMods...)
}

// PortalRolePermissions retrieves all the portal_role_permission's PortalRolePermissions with an executor.
func (o *PortalRole) PortalRolePermissions(mods ...qm.QueryMod) portalRolePermissionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"portal_role_permissions\".\"portal_role_id\"=?", o.ID),
	)

	return PortalRolePermissions(queryMods...)
}

// LoadCreatedByUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (portalRoleL) LoadCreatedByUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		args[object.CreatedBy] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}

			args[obj.CreatedBy] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.CreatedByUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.CreatedByPortalRoles = append(foreign.R.CreatedByPortalRoles, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.CreatedBy == foreign.ID {
				local.R.CreatedByUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.CreatedByPortalRoles = append(foreign.R.CreatedByPortalRoles, local)
				break
			}
		}
	}

	return nil
}

// LoadPortal allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (portalRoleL) LoadPortal(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		args[object.PortalID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}

			args[obj.PortalID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portals`),
		qm.WhereIn(`portals.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Portal")
	}

	var resultSlice []*Portal
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Portal")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for portals")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portals")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Portal = foreign
		if foreign.R == nil {
			foreign.R = &portalR{}
		}
		foreign.R.PortalRoles = append(foreign.R.PortalRoles, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PortalID == foreign.ID {
				local.R.Portal = foreign
				if foreign.R == nil {
					foreign.R = &portalR{}
				}
				foreign.R.PortalRoles = append(foreign.R.PortalRoles, local)
				break
			}
		}
	}

	return nil
}

// LoadUpdatedByUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (portalRoleL) LoadUpdatedByUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		if !queries.IsNil(object.UpdatedBy) {
			args[object.UpdatedBy] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}

			if !queries.IsNil(obj.UpdatedBy) {
				args[obj.UpdatedBy] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.UpdatedByUser = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.UpdatedByPortalRoles = append(foreign.R.UpdatedByPortalRoles, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.UpdatedBy, foreign.ID) {
				local.R.UpdatedByUser = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.UpdatedByPortalRoles = append(foreign.R.UpdatedByPortalRoles, local)
				break
			}
		}
	}

	return nil
}

// LoadDocumentPermissionOverrides allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (portalRoleL) LoadDocumentPermissionOverrides(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`document_permission_overrides`),
		qm.WhereIn(`document_permission_overrides.portal_role_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load document_permission_overrides")
	}

	var resultSlice []*DocumentPermissionOverride
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice document_permission_overrides")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on document_permission_overrides")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for document_permission_overrides")
	}

	if singular {
		object.R.DocumentPermissionOverrides = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &documentPermissionOverrideR{}
			}
			foreign.R.PortalRole = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.PortalRoleID) {
				local.R.DocumentPermissionOverrides = append(local.R.DocumentPermissionOverrides, foreign)
				if foreign.R == nil {
					foreign.R = &documentPermissionOverrideR{}
				}
				foreign.R.PortalRole = local
				break
			}
		}
	}

	return nil
}

// LoadFolderPermissionOverrides allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (portalRoleL) LoadFolderPermissionOverrides(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`folder_permission_overrides`),
		qm.WhereIn(`folder_permission_overrides.portal_role_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load folder_permission_overrides")
	}

	var resultSlice []*FolderPermissionOverride
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice folder_permission_overrides")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on folder_permission_overrides")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for folder_permission_overrides")
	}

	if singular {
		object.R.FolderPermissionOverrides = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &folderPermissionOverrideR{}
			}
			foreign.R.PortalRole = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.PortalRoleID) {
				local.R.FolderPermissionOverrides = append(local.R.FolderPermissionOverrides, foreign)
				if foreign.R == nil {
					foreign.R = &folderPermissionOverrideR{}
				}
				foreign.R.PortalRole = local
				break
			}
		}
	}

	return nil
}

// LoadPortalMemberships allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (portalRoleL) LoadPortalMemberships(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_memberships`),
		qm.WhereIn(`portal_memberships.portal_role_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portal_memberships")
	}

	var resultSlice []*PortalMembership
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portal_memberships")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portal_memberships")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_memberships")
	}

	if singular {
		object.R.PortalMemberships = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalMembershipR{}
			}
			foreign.R.PortalRole = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PortalRoleID {
				local.R.PortalMemberships = append(local.R.PortalMemberships, foreign)
				if foreign.R == nil {
					foreign.R = &portalMembershipR{}
				}
				foreign.R.PortalRole = local
				break
			}
		}
	}

	return nil
}

// LoadPortalRolePermissions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (portalRoleL) LoadPortalRolePermissions(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalRole any, mods queries.Applicator) error {
	var slice []*PortalRole
	var object *PortalRole

	if singular {
		var ok bool
		object, ok = maybePortalRole.(*PortalRole)
		if !ok {
			object = new(PortalRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalRole))
			}
		}
	} else {
		s, ok := maybePortalRole.(*[]*PortalRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalRoleR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalRoleR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_role_permissions`),
		qm.WhereIn(`portal_role_permissions.portal_role_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load portal_role_permissions")
	}

	var resultSlice []*PortalRolePermission
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice portal_role_permissions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on portal_role_permissions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_role_permissions")
	}

	if singular {
		object.R.PortalRolePermissions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &portalRolePermissionR{}
			}
			foreign.R.PortalRole = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.PortalRoleID {
				local.R.PortalRolePermissions = append(local.R.PortalRolePermissions, foreign)
				if foreign.R == nil {
					foreign.R = &portalRolePermissionR{}
				}
				foreign.R.PortalRole = local
				break
			}
		}
	}

	return nil
}

// SetCreatedByUser of the portalRole to the related item.
// Sets o.R.CreatedByUser to related.
// Adds o to related.R.CreatedByPortalRoles.
func (o *PortalRole) SetCreatedByUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"portal_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"created_by"}),
		strmangle.WhereClause("\"", "\"", 2, portalRolePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.CreatedBy = related.ID
	if o.R == nil {
		o.R = &portalRoleR{
			CreatedByUser: related,
		}
	} else {
		o.R.CreatedByUser = related
	}

	if related.R == nil {
		related.R = &userR{
			CreatedByPortalRoles: PortalRoleSlice{o},
		}
	} else {
		related.R.CreatedByPortalRoles = append(related.R.CreatedByPortalRoles, o)
	}

	return nil
}

// SetPortal of the portalRole to the related item.
// Sets o.R.Portal to related.
// Adds o to related.R.PortalRoles.
func (o *PortalRole) SetPortal(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Portal) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"portal_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"portal_id"}),
		strmangle.WhereClause("\"", "\"", 2, portalRolePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PortalID = related.ID
	if o.R == nil {
		o.R = &portalRoleR{
			Portal: related,
		}
	} else {
		o.R.Portal = related
	}

	if related.R == nil {
		related.R = &portalR{
			PortalRoles: PortalRoleSlice{o},
		}
	} else {
		related.R.PortalRoles = append(related.R.PortalRoles, o)
	}

	return nil
}

// SetUpdatedByUser of the portalRole to the related item.
// Sets o.R.UpdatedByUser to related.
// Adds o to related.R.UpdatedByPortalRoles.
func (o *PortalRole) SetUpdatedByUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"portal_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"updated_by"}),
		strmangle.WhereClause("\"", "\"", 2, portalRolePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.UpdatedBy, related.ID)
	if o.R == nil {
		o.R = &portalRoleR{
			UpdatedByUser: related,
		}
	} else {
		o.R.UpdatedByUser = related
	}

	if related.R == nil {
		related.R = &userR{
			UpdatedByPortalRoles: PortalRoleSlice{o},
		}
	} else {
		related.R.UpdatedByPortalRoles = append(related.R.UpdatedByPortalRoles, o)
	}

	return nil
}

// RemoveUpdatedByUser relationship.
// Sets o.R.UpdatedByUser to nil.
// Removes o from all passed in related items' relationships struct.
func (o *PortalRole) RemoveUpdatedByUser(ctx context.Context, exec boil.ContextExecutor, related *User) error {
	var err error

	queries.SetScanner(&o.UpdatedBy, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("updated_by")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.UpdatedByUser = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.UpdatedByPortalRoles {
		if queries.Equal(o.UpdatedBy, ri.UpdatedBy) {
			continue
		}

		ln := len(related.R.UpdatedByPortalRoles)
		if ln > 1 && i < ln-1 {
			related.R.UpdatedByPortalRoles[i] = related.R.UpdatedByPortalRoles[ln-1]
		}
		related.R.UpdatedByPortalRoles = related.R.UpdatedByPortalRoles[:ln-1]
		break
	}
	return nil
}

// AddDocumentPermissionOverrides adds the given related objects to the existing relationships
// of the portal_role, optionally inserting them as new records.
// Appends related to o.R.DocumentPermissionOverrides.
// Sets related.R.PortalRole appropriately.
func (o *PortalRole) AddDocumentPermissionOverrides(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentPermissionOverride) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.PortalRoleID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"document_permission_overrides\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"portal_role_id"}),
				strmangle.WhereClause("\"", "\"", 2, documentPermissionOverridePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.PortalRoleID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &portalRoleR{
			DocumentPermissionOverrides: related,
		}
	} else {
		o.R.DocumentPermissionOverrides = append(o.R.DocumentPermissionOverrides, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &documentPermissionOverrideR{
				PortalRole: o,
			}
		} else {
			rel.R.PortalRole = o
		}
	}
	return nil
}

// SetDocumentPermissionOverrides removes all previously related items of the
// portal_role replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.PortalRole's DocumentPermissionOverrides accordingly.
// Replaces o.R.DocumentPermissionOverrides with related.
// Sets related.R.PortalRole's DocumentPermissionOverrides accordingly.
func (o *PortalRole) SetDocumentPermissionOverrides(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*DocumentPermissionOverride) error {
	query := "update \"document_permission_overrides\" set \"portal_role_id\" = null where \"portal_role_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.DocumentPermissionOverrides {
			queries.SetScanner(&rel.PortalRoleID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.PortalRole = nil
		}
		o.R.DocumentPermissionOverrides = nil
	}

	return o.AddDocumentPermissionOverrides(ctx, exec, insert, related...)
}

// RemoveDocumentPermissionOverrides relationships from objects passed in.
// Removes related items from R.DocumentPermissionOverrides (uses pointer comparison, removal does not keep order)
// Sets related.R.PortalRole.
func (o *PortalRole) RemoveDocumentPermissionOverrides(ctx context.Context, exec boil.ContextExecutor, related ...*DocumentPermissionOverride) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.PortalRoleID, nil)
		if rel.R != nil {
			rel.R.PortalRole = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("portal_role_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.DocumentPermissionOverrides {
			if rel != ri {
				continue
			}

			ln := len(o.R.DocumentPermissionOverrides)
			if ln > 1 && i < ln-1 {
				o.R.DocumentPermissionOverrides[i] = o.R.DocumentPermissionOverrides[ln-1]
			}
			o.R.DocumentPermissionOverrides = o.R.DocumentPermissionOverrides[:ln-1]
			break
		}
	}

	return nil
}

// AddFolderPermissionOverrides adds the given related objects to the existing relationships
// of the portal_role, optionally inserting them as new records.
// Appends related to o.R.FolderPermissionOverrides.
// Sets related.R.PortalRole appropriately.
func (o *PortalRole) AddFolderPermissionOverrides(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FolderPermissionOverride) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.PortalRoleID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"folder_permission_overrides\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"portal_role_id"}),
				strmangle.WhereClause("\"", "\"", 2, folderPermissionOverridePrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.PortalRoleID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &portalRoleR{
			FolderPermissionOverrides: related,
		}
	} else {
		o.R.FolderPermissionOverrides = append(o.R.FolderPermissionOverrides, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &folderPermissionOverrideR{
				PortalRole: o,
			}
		} else {
			rel.R.PortalRole = o
		}
	}
	return nil
}

// SetFolderPermissionOverrides removes all previously related items of the
// portal_role replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.PortalRole's FolderPermissionOverrides accordingly.
// Replaces o.R.FolderPermissionOverrides with related.
// Sets related.R.PortalRole's FolderPermissionOverrides accordingly.
func (o *PortalRole) SetFolderPermissionOverrides(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*FolderPermissionOverride) error {
	query := "update \"folder_permission_overrides\" set \"portal_role_id\" = null where \"portal_role_id\" = $1"
	values := []any{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.FolderPermissionOverrides {
			queries.SetScanner(&rel.PortalRoleID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.PortalRole = nil
		}
		o.R.FolderPermissionOverrides = nil
	}

	return o.AddFolderPermissionOverrides(ctx, exec, insert, related...)
}

// RemoveFolderPermissionOverrides relationships from objects passed in.
// Removes related items from R.FolderPermissionOverrides (uses pointer comparison, removal does not keep order)
// Sets related.R.PortalRole.
func (o *PortalRole) RemoveFolderPermissionOverrides(ctx context.Context, exec boil.ContextExecutor, related ...*FolderPermissionOverride) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.PortalRoleID, nil)
		if rel.R != nil {
			rel.R.PortalRole = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("portal_role_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.FolderPermissionOverrides {
			if rel != ri {
				continue
			}

			ln := len(o.R.FolderPermissionOverrides)
			if ln > 1 && i < ln-1 {
				o.R.FolderPermissionOverrides[i] = o.R.FolderPermissionOverrides[ln-1]
			}
			o.R.FolderPermissionOverrides = o.R.FolderPermissionOverrides[:ln-1]
			break
		}
	}

	return nil
}

// AddPortalMemberships adds the given related objects to the existing relationships
// of the portal_role, optionally inserting them as new records.
// Appends related to o.R.PortalMemberships.
// Sets related.R.PortalRole appropriately.
func (o *PortalRole) AddPortalMemberships(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PortalMembership) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PortalRoleID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portal_memberships\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"portal_role_id"}),
				strmangle.WhereClause("\"", "\"", 2, portalMembershipPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PortalRoleID = o.ID
		}
	}

	if o.R == nil {
		o.R = &portalRoleR{
			PortalMemberships: related,
		}
	} else {
		o.R.PortalMemberships = append(o.R.PortalMemberships, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalMembershipR{
				PortalRole: o,
			}
		} else {
			rel.R.PortalRole = o
		}
	}
	return nil
}

// AddPortalRolePermissions adds the given related objects to the existing relationships
// of the portal_role, optionally inserting them as new records.
// Appends related to o.R.PortalRolePermissions.
// Sets related.R.PortalRole appropriately.
func (o *PortalRole) AddPortalRolePermissions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PortalRolePermission) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.PortalRoleID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"portal_role_permissions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"portal_role_id"}),
				strmangle.WhereClause("\"", "\"", 2, portalRolePermissionPrimaryKeyColumns),
			)
			values := []any{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.PortalRoleID = o.ID
		}
	}

	if o.R == nil {
		o.R = &portalRoleR{
			PortalRolePermissions: related,
		}
	} else {
		o.R.PortalRolePermissions = append(o.R.PortalRolePermissions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &portalRolePermissionR{
				PortalRole: o,
			}
		} else {
			rel.R.PortalRole = o
		}
	}
	return nil
}

// PortalRoles retrieves all the records using an executor.
func PortalRoles(mods ...qm.QueryMod) portalRoleQuery {
	mods = append(mods, qm.From("\"portal_roles\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"portal_roles\".*"})
	}

	return portalRoleQuery{q}
}

// FindPortalRole retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPortalRole(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*PortalRole, error) {
	portalRoleObj := &PortalRole{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"portal_roles\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, portalRoleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from portal_roles")
	}

	return portalRoleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PortalRole) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no portal_roles provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(portalRoleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	portalRoleInsertCacheMut.RLock()
	cache, cached := portalRoleInsertCache[key]
	portalRoleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			portalRoleAllColumns,
			portalRoleColumnsWithDefault,
			portalRoleColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, portalRoleGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(portalRoleType, portalRoleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(portalRoleType, portalRoleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"portal_roles\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"portal_roles\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into portal_roles")
	}

	if !cached {
		portalRoleInsertCacheMut.Lock()
		portalRoleInsertCache[key] = cache
		portalRoleInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the PortalRole.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PortalRole) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	key := makeCacheKey(columns, nil)
	portalRoleUpdateCacheMut.RLock()
	cache, cached := portalRoleUpdateCache[key]
	portalRoleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			portalRoleAllColumns,
			portalRolePrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, portalRoleGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update portal_roles, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"portal_roles\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, portalRolePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(portalRoleType, portalRoleMapping, append(wl, portalRolePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update portal_roles row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for portal_roles")
	}

	if !cached {
		portalRoleUpdateCacheMut.Lock()
		portalRoleUpdateCache[key] = cache
		portalRoleUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q portalRoleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for portal_roles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for portal_roles")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PortalRoleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), portalRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"portal_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, portalRolePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in portalRole slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all portalRole")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PortalRole) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no portal_roles provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	nzDefaults := queries.NonZeroDefaultSet(portalRoleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	portalRoleUpsertCacheMut.RLock()
	cache, cached := portalRoleUpsertCache[key]
	portalRoleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			portalRoleAllColumns,
			portalRoleColumnsWithDefault,
			portalRoleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			portalRoleAllColumns,
			portalRolePrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, portalRoleGeneratedColumns)
		update = strmangle.SetComplement(update, portalRoleGeneratedColumns)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert portal_roles, could not build update column list")
		}

		ret := strmangle.SetComplement(portalRoleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(portalRolePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert portal_roles, could not build conflict column list")
			}

			conflict = make([]string, len(portalRolePrimaryKeyColumns))
			copy(conflict, portalRolePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"portal_roles\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(portalRoleType, portalRoleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(portalRoleType, portalRoleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert portal_roles")
	}

	if !cached {
		portalRoleUpsertCacheMut.Lock()
		portalRoleUpsertCache[key] = cache
		portalRoleUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single PortalRole record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PortalRole) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PortalRole provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), portalRolePrimaryKeyMapping)
	sql := "DELETE FROM \"portal_roles\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from portal_roles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for portal_roles")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q portalRoleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no portalRoleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from portal_roles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for portal_roles")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PortalRoleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), portalRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"portal_roles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, portalRolePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from portalRole slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for portal_roles")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PortalRole) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPortalRole(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PortalRoleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PortalRoleSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), portalRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"portal_roles\".* FROM \"portal_roles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, portalRolePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PortalRoleSlice")
	}

	*o = slice

	return nil
}

// PortalRoleExists checks if the PortalRole row exists.
func PortalRoleExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"portal_roles\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if portal_roles exists")
	}

	return exists, nil
}

// Exists checks if the PortalRole row exists.
func (o *PortalRole) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PortalRoleExists(ctx, exec, o.ID)
}
