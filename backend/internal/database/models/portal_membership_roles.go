// Code generated by SQLBoiler 4.19.7 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// PortalMembershipRole is an object representing the database table.
type PortalMembershipRole struct {
	ID                 int64     `boil:"id" json:"id" toml:"id" yaml:"id"`
	PortalMembershipID int64     `boil:"portal_membership_id" json:"portal_membership_id" toml:"portal_membership_id" yaml:"portal_membership_id"`
	PortalRoleID       int64     `boil:"portal_role_id" json:"portal_role_id" toml:"portal_role_id" yaml:"portal_role_id"`
	CreatedAt          time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *portalMembershipRoleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L portalMembershipRoleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PortalMembershipRoleColumns = struct {
	ID                 string
	PortalMembershipID string
	PortalRoleID       string
	CreatedAt          string
}{
	ID:                 "id",
	PortalMembershipID: "portal_membership_id",
	PortalRoleID:       "portal_role_id",
	CreatedAt:          "created_at",
}

var PortalMembershipRoleTableColumns = struct {
	ID                 string
	PortalMembershipID string
	PortalRoleID       string
	CreatedAt          string
}{
	ID:                 "portal_membership_roles.id",
	PortalMembershipID: "portal_membership_roles.portal_membership_id",
	PortalRoleID:       "portal_membership_roles.portal_role_id",
	CreatedAt:          "portal_membership_roles.created_at",
}

// Generated where

var PortalMembershipRoleWhere = struct {
	ID                 whereHelperint64
	PortalMembershipID whereHelperint64
	PortalRoleID       whereHelperint64
	CreatedAt          whereHelpertime_Time
}{
	ID:                 whereHelperint64{field: "\"portal_membership_roles\".\"id\""},
	PortalMembershipID: whereHelperint64{field: "\"portal_membership_roles\".\"portal_membership_id\""},
	PortalRoleID:       whereHelperint64{field: "\"portal_membership_roles\".\"portal_role_id\""},
	CreatedAt:          whereHelpertime_Time{field: "\"portal_membership_roles\".\"created_at\""},
}

// PortalMembershipRoleRels is where relationship names are stored.
var PortalMembershipRoleRels = struct {
	PortalMembership string
	PortalRole       string
}{
	PortalMembership: "PortalMembership",
	PortalRole:       "PortalRole",
}

// portalMembershipRoleR is where relationships are stored.
type portalMembershipRoleR struct {
	PortalMembership *PortalMembership `boil:"PortalMembership" json:"PortalMembership" toml:"PortalMembership" yaml:"PortalMembership"`
	PortalRole       *PortalRole       `boil:"PortalRole" json:"PortalRole" toml:"PortalRole" yaml:"PortalRole"`
}

// NewStruct creates a new relationship struct
func (*portalMembershipRoleR) NewStruct() *portalMembershipRoleR {
	return &portalMembershipRoleR{}
}

func (o *PortalMembershipRole) GetPortalMembership() *PortalMembership {
	if o == nil {
		return nil
	}

	return o.R.GetPortalMembership()
}

func (r *portalMembershipRoleR) GetPortalMembership() *PortalMembership {
	if r == nil {
		return nil
	}

	return r.PortalMembership
}

func (o *PortalMembershipRole) GetPortalRole() *PortalRole {
	if o == nil {
		return nil
	}

	return o.R.GetPortalRole()
}

func (r *portalMembershipRoleR) GetPortalRole() *PortalRole {
	if r == nil {
		return nil
	}

	return r.PortalRole
}

// portalMembershipRoleL is where Load methods for each relationship are stored.
type portalMembershipRoleL struct{}

var (
	portalMembershipRoleAllColumns            = []string{"id", "portal_membership_id", "portal_role_id", "created_at"}
	portalMembershipRoleColumnsWithoutDefault = []string{"portal_membership_id", "portal_role_id"}
	portalMembershipRoleColumnsWithDefault    = []string{"id", "created_at"}
	portalMembershipRolePrimaryKeyColumns     = []string{"id"}
	portalMembershipRoleGeneratedColumns      = []string{"id"}
)

type (
	// PortalMembershipRoleSlice is an alias for a slice of pointers to PortalMembershipRole.
	// This should almost always be used instead of []PortalMembershipRole.
	PortalMembershipRoleSlice []*PortalMembershipRole

	portalMembershipRoleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	portalMembershipRoleType                 = reflect.TypeOf(&PortalMembershipRole{})
	portalMembershipRoleMapping              = queries.MakeStructMapping(portalMembershipRoleType)
	portalMembershipRolePrimaryKeyMapping, _ = queries.BindMapping(portalMembershipRoleType, portalMembershipRoleMapping, portalMembershipRolePrimaryKeyColumns)
	portalMembershipRoleInsertCacheMut       sync.RWMutex
	portalMembershipRoleInsertCache          = make(map[string]insertCache)
	portalMembershipRoleUpdateCacheMut       sync.RWMutex
	portalMembershipRoleUpdateCache          = make(map[string]updateCache)
	portalMembershipRoleUpsertCacheMut       sync.RWMutex
	portalMembershipRoleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

// One returns a single portalMembershipRole record from the query.
func (q portalMembershipRoleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PortalMembershipRole, error) {
	o := &PortalMembershipRole{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for portal_membership_roles")
	}

	return o, nil
}

// All returns all PortalMembershipRole records from the query.
func (q portalMembershipRoleQuery) All(ctx context.Context, exec boil.ContextExecutor) (PortalMembershipRoleSlice, error) {
	var o []*PortalMembershipRole

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to PortalMembershipRole slice")
	}

	return o, nil
}

// Count returns the count of all PortalMembershipRole records in the query.
func (q portalMembershipRoleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count portal_membership_roles rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q portalMembershipRoleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if portal_membership_roles exists")
	}

	return count > 0, nil
}

// PortalMembership pointed to by the foreign key.
func (o *PortalMembershipRole) PortalMembership(mods ...qm.QueryMod) portalMembershipQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PortalMembershipID),
	}

	queryMods = append(queryMods, mods...)

	return PortalMemberships(queryMods...)
}

// PortalRole pointed to by the foreign key.
func (o *PortalMembershipRole) PortalRole(mods ...qm.QueryMod) portalRoleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PortalRoleID),
	}

	queryMods = append(queryMods, mods...)

	return PortalRoles(queryMods...)
}

// LoadPortalMembership allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (portalMembershipRoleL) LoadPortalMembership(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalMembershipRole any, mods queries.Applicator) error {
	var slice []*PortalMembershipRole
	var object *PortalMembershipRole

	if singular {
		var ok bool
		object, ok = maybePortalMembershipRole.(*PortalMembershipRole)
		if !ok {
			object = new(PortalMembershipRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalMembershipRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalMembershipRole))
			}
		}
	} else {
		s, ok := maybePortalMembershipRole.(*[]*PortalMembershipRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalMembershipRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalMembershipRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalMembershipRoleR{}
		}
		args[object.PortalMembershipID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalMembershipRoleR{}
			}

			args[obj.PortalMembershipID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_memberships`),
		qm.WhereIn(`portal_memberships.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PortalMembership")
	}

	var resultSlice []*PortalMembership
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PortalMembership")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for portal_memberships")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_memberships")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PortalMembership = foreign
		if foreign.R == nil {
			foreign.R = &portalMembershipR{}
		}
		foreign.R.PortalMembershipRoles = append(foreign.R.PortalMembershipRoles, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PortalMembershipID == foreign.ID {
				local.R.PortalMembership = foreign
				if foreign.R == nil {
					foreign.R = &portalMembershipR{}
				}
				foreign.R.PortalMembershipRoles = append(foreign.R.PortalMembershipRoles, local)
				break
			}
		}
	}

	return nil
}

// LoadPortalRole allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (portalMembershipRoleL) LoadPortalRole(ctx context.Context, e boil.ContextExecutor, singular bool, maybePortalMembershipRole any, mods queries.Applicator) error {
	var slice []*PortalMembershipRole
	var object *PortalMembershipRole

	if singular {
		var ok bool
		object, ok = maybePortalMembershipRole.(*PortalMembershipRole)
		if !ok {
			object = new(PortalMembershipRole)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePortalMembershipRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePortalMembershipRole))
			}
		}
	} else {
		s, ok := maybePortalMembershipRole.(*[]*PortalMembershipRole)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePortalMembershipRole)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePortalMembershipRole))
			}
		}
	}

	args := make(map[any]struct{})
	if singular {
		if object.R == nil {
			object.R = &portalMembershipRoleR{}
		}
		args[object.PortalRoleID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &portalMembershipRoleR{}
			}

			args[obj.PortalRoleID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]any, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`portal_roles`),
		qm.WhereIn(`portal_roles.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PortalRole")
	}

	var resultSlice []*PortalRole
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PortalRole")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for portal_roles")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for portal_roles")
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.PortalRole = foreign
		if foreign.R == nil {
			foreign.R = &portalRoleR{}
		}
		foreign.R.PortalMembershipRoles = append(foreign.R.PortalMembershipRoles, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PortalRoleID == foreign.ID {
				local.R.PortalRole = foreign
				if foreign.R == nil {
					foreign.R = &portalRoleR{}
				}
				foreign.R.PortalMembershipRoles = append(foreign.R.PortalMembershipRoles, local)
				break
			}
		}
	}

	return nil
}

// SetPortalMembership of the portalMembershipRole to the related item.
// Sets o.R.PortalMembership to related.
// Adds o to related.R.PortalMembershipRoles.
func (o *PortalMembershipRole) SetPortalMembership(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PortalMembership) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"portal_membership_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"portal_membership_id"}),
		strmangle.WhereClause("\"", "\"", 2, portalMembershipRolePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PortalMembershipID = related.ID
	if o.R == nil {
		o.R = &portalMembershipRoleR{
			PortalMembership: related,
		}
	} else {
		o.R.PortalMembership = related
	}

	if related.R == nil {
		related.R = &portalMembershipR{
			PortalMembershipRoles: PortalMembershipRoleSlice{o},
		}
	} else {
		related.R.PortalMembershipRoles = append(related.R.PortalMembershipRoles, o)
	}

	return nil
}

// SetPortalRole of the portalMembershipRole to the related item.
// Sets o.R.PortalRole to related.
// Adds o to related.R.PortalMembershipRoles.
func (o *PortalMembershipRole) SetPortalRole(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PortalRole) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"portal_membership_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"portal_role_id"}),
		strmangle.WhereClause("\"", "\"", 2, portalMembershipRolePrimaryKeyColumns),
	)
	values := []any{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PortalRoleID = related.ID
	if o.R == nil {
		o.R = &portalMembershipRoleR{
			PortalRole: related,
		}
	} else {
		o.R.PortalRole = related
	}

	if related.R == nil {
		related.R = &portalRoleR{
			PortalMembershipRoles: PortalMembershipRoleSlice{o},
		}
	} else {
		related.R.PortalMembershipRoles = append(related.R.PortalMembershipRoles, o)
	}

	return nil
}

// PortalMembershipRoles retrieves all the records using an executor.
func PortalMembershipRoles(mods ...qm.QueryMod) portalMembershipRoleQuery {
	mods = append(mods, qm.From("\"portal_membership_roles\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"portal_membership_roles\".*"})
	}

	return portalMembershipRoleQuery{q}
}

// FindPortalMembershipRole retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPortalMembershipRole(ctx context.Context, exec boil.ContextExecutor, iD int64, selectCols ...string) (*PortalMembershipRole, error) {
	portalMembershipRoleObj := &PortalMembershipRole{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"portal_membership_roles\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, portalMembershipRoleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from portal_membership_roles")
	}

	return portalMembershipRoleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PortalMembershipRole) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no portal_membership_roles provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(portalMembershipRoleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	portalMembershipRoleInsertCacheMut.RLock()
	cache, cached := portalMembershipRoleInsertCache[key]
	portalMembershipRoleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			portalMembershipRoleAllColumns,
			portalMembershipRoleColumnsWithDefault,
			portalMembershipRoleColumnsWithoutDefault,
			nzDefaults,
		)
		wl = strmangle.SetComplement(wl, portalMembershipRoleGeneratedColumns)

		cache.valueMapping, err = queries.BindMapping(portalMembershipRoleType, portalMembershipRoleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(portalMembershipRoleType, portalMembershipRoleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"portal_membership_roles\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"portal_membership_roles\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into portal_membership_roles")
	}

	if !cached {
		portalMembershipRoleInsertCacheMut.Lock()
		portalMembershipRoleInsertCache[key] = cache
		portalMembershipRoleInsertCacheMut.Unlock()
	}

	return nil
}

// Update uses an executor to update the PortalMembershipRole.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PortalMembershipRole) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	key := makeCacheKey(columns, nil)
	portalMembershipRoleUpdateCacheMut.RLock()
	cache, cached := portalMembershipRoleUpdateCache[key]
	portalMembershipRoleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			portalMembershipRoleAllColumns,
			portalMembershipRolePrimaryKeyColumns,
		)
		wl = strmangle.SetComplement(wl, portalMembershipRoleGeneratedColumns)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update portal_membership_roles, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"portal_membership_roles\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, portalMembershipRolePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(portalMembershipRoleType, portalMembershipRoleMapping, append(wl, portalMembershipRolePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update portal_membership_roles row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for portal_membership_roles")
	}

	if !cached {
		portalMembershipRoleUpdateCacheMut.Lock()
		portalMembershipRoleUpdateCache[key] = cache
		portalMembershipRoleUpdateCacheMut.Unlock()
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values.
func (q portalMembershipRoleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for portal_membership_roles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for portal_membership_roles")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PortalMembershipRoleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]any, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), portalMembershipRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"portal_membership_roles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, portalMembershipRolePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in portalMembershipRole slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all portalMembershipRole")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PortalMembershipRole) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no portal_membership_roles provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	nzDefaults := queries.NonZeroDefaultSet(portalMembershipRoleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	portalMembershipRoleUpsertCacheMut.RLock()
	cache, cached := portalMembershipRoleUpsertCache[key]
	portalMembershipRoleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			portalMembershipRoleAllColumns,
			portalMembershipRoleColumnsWithDefault,
			portalMembershipRoleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			portalMembershipRoleAllColumns,
			portalMembershipRolePrimaryKeyColumns,
		)

		insert = strmangle.SetComplement(insert, portalMembershipRoleGeneratedColumns)
		update = strmangle.SetComplement(update, portalMembershipRoleGeneratedColumns)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert portal_membership_roles, could not build update column list")
		}

		ret := strmangle.SetComplement(portalMembershipRoleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(portalMembershipRolePrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert portal_membership_roles, could not build conflict column list")
			}

			conflict = make([]string, len(portalMembershipRolePrimaryKeyColumns))
			copy(conflict, portalMembershipRolePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"portal_membership_roles\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(portalMembershipRoleType, portalMembershipRoleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(portalMembershipRoleType, portalMembershipRoleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []any
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert portal_membership_roles")
	}

	if !cached {
		portalMembershipRoleUpsertCacheMut.Lock()
		portalMembershipRoleUpsertCache[key] = cache
		portalMembershipRoleUpsertCacheMut.Unlock()
	}

	return nil
}

// Delete deletes a single PortalMembershipRole record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PortalMembershipRole) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no PortalMembershipRole provided for delete")
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), portalMembershipRolePrimaryKeyMapping)
	sql := "DELETE FROM \"portal_membership_roles\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from portal_membership_roles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for portal_membership_roles")
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q portalMembershipRoleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no portalMembershipRoleQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from portal_membership_roles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for portal_membership_roles")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PortalMembershipRoleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	var args []any
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), portalMembershipRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"portal_membership_roles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, portalMembershipRolePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from portalMembershipRole slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for portal_membership_roles")
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PortalMembershipRole) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPortalMembershipRole(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PortalMembershipRoleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PortalMembershipRoleSlice{}
	var args []any
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), portalMembershipRolePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"portal_membership_roles\".* FROM \"portal_membership_roles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, portalMembershipRolePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in PortalMembershipRoleSlice")
	}

	*o = slice

	return nil
}

// PortalMembershipRoleExists checks if the PortalMembershipRole row exists.
func PortalMembershipRoleExists(ctx context.Context, exec boil.ContextExecutor, iD int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"portal_membership_roles\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if portal_membership_roles exists")
	}

	return exists, nil
}

// Exists checks if the PortalMembershipRole row exists.
func (o *PortalMembershipRole) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PortalMembershipRoleExists(ctx, exec, o.ID)
}
